DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/testdb
PORT=8000
ENV=development
# This is a test environment configuration file.
# It is used to set environment variables for testing purposes.
# The DATABASE_URL is set to a test database.
# The PORT is set to 8000 for testing.
# The ENV is set to development to indicate that this is a development environment.